# EcoCred Environment Configuration Template
# Copy this file to create your environment-specific .env files

# =============================================================================
# BLOCKCHAIN CONFIGURATION (blockchain/.env)
# =============================================================================

# Hardhat Network RPC URL (for local development)
HARDHAT_RPC_URL=http://localhost:8545

# Sepolia Testnet Configuration (for testnet deployment)
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID
SEPOLIA_PRIVATE_KEY=your_private_key_here

# Etherscan API Key (for contract verification)
ETHERSCAN_API_KEY=your_etherscan_api_key

# =============================================================================
# BACKEND CONFIGURATION (backend/.env)
# =============================================================================

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/ecocred
# Example Neon PostgreSQL:
# DATABASE_URL=postgresql://username:password@ep-xxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# JWT Secret (generate a random string for production)
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# Server Configuration
PORT=3001
NODE_ENV=development

# Blockchain Network Configuration
BLOCKCHAIN_RPC_URL=http://localhost:8545

# Contract Addresses (auto-detected from deployment artifacts - no need to set manually)
# CONTRACT_ADDRESS_CARBON=0x...
# CONTRACT_ADDRESS_BADGE=0x...
# CONTRACT_ADDRESS_LEDGER=0x...
# CONTRACT_ADDRESS_MARKETPLACE=0x...
# CONTRACT_ADDRESS_STAKING=0x...
# CONTRACT_ADDRESS_GOVERNANCE=0x...

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Logging
LOG_LEVEL=info

# =============================================================================
# FRONTEND CONFIGURATION (frontend-react/.env)
# =============================================================================

# API Base URL
VITE_API_BASE_URL=http://localhost:3001

# Blockchain Network
VITE_CHAIN_ID=31337
VITE_NETWORK_NAME=localhost

# Contract addresses are auto-detected from deployment artifacts
# No manual configuration needed!

# =============================================================================
# PRODUCTION NOTES
# =============================================================================

# For production deployment:
# 1. Use strong, randomly generated JWT_SECRET
# 2. Update DATABASE_URL to production database
# 3. Set BLOCKCHAIN_RPC_URL to mainnet or production network
# 4. Update ALLOWED_ORIGINS to your production domain
# 5. Set NODE_ENV=production
# 6. Never commit actual .env files to version control
# 7. Use environment variables in your deployment platform (Vercel, Railway, etc.)
