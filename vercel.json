{
    "version": 2,
    "builds": [
        {
            "src": "backend/src/server.ts",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "backend/src/**",
                    "backend/src/database/schema.prisma"
                ]
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "backend/src/server.ts"
        },
        {
            "src": "/(.*)",
            "dest": "backend/src/server.ts"
        }
    ],
    "env": {
        "NODE_ENV": "production"
    }
}